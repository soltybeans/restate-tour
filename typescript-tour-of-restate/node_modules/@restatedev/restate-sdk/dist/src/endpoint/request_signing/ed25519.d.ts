/// <reference types="node" />
/// <reference types="node" />
import { Buffer } from "node:buffer";
import { webcrypto } from "node:crypto";
import * as crypto from "node:crypto";
export type Key = {
    type: "web";
    key: Promise<webcrypto.CryptoKey>;
    kid: string;
} | {
    type: "node";
    key: crypto.KeyObject;
    kid: string;
};
export declare function importKey(kid: string, derBytes: Buffer): Key;
export declare function verify(key: Key, signatureBuf: Buffer, data: Buffer): Promise<boolean>;
//# sourceMappingURL=ed25519.d.ts.map